for (int idx = 0; idx < barazk.length(); ++idx) {
   msgChar = msg.charAt(idx);
   if(msgChar = '.'){
    puntu = false;
   }
}
        while (i < max || !puntu) {
            barazk[i] = in.next();
            if (barazk[i] == ".") {
                puntu = false;
            }
            i++;
        }